# 원격 GPU 서버 연결 정보 (GitHub에 업로드되지 않음)
# 이 파일을 remote.env로 복사 후 실제 값으로 채우세요.
# cp remote.env.example remote.env

REMOTE_HOST=your-remote-host
REMOTE_USER=root
REMOTE_PORT=22
SSH_KEY=${HOME}/.ssh/your_key.pem
